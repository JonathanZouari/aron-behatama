{
  "name": "aron-behatama-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "ממשק הלקוח והנגר של ״ארון בהתאמה״ — קבצים סטטיים + reverse proxy ל-/api",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "node -e \"console.log('no build step: static files served as-is')\"",
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "http-proxy-middleware": "^3.0.5"
  }
}
